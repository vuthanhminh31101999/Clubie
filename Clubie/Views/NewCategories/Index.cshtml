@model IEnumerable<Clubie.Models.NewCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NewCategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NewCategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NewCategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.NewCategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NewCategoryId })
        </td>
    </tr>
}

</table>
